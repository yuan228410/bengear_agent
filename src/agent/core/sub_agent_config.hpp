#pragma once

// 已迁移至 base/config/sub_agent_config.hpp（P0 分层修复）
#include "base/config/sub_agent_config.hpp"

namespace ben_gear {
using SubAgentConfig = config::SubAgentConfig;
using SessionType = config::SessionType;
}